apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base

commonLabels:
  env: dev

images:
  - name: inventory-service
    newName: asia-southeast1-docker.pkg.dev/YOUR_GCP_PROJECT/YOUR_REPO/inventory-service
    newTag: latest

replicas:
  - name: inventory-service
    count: 1

configMapGenerator:
  - name: inventory-service-config
    envs:
      - .env

secretGenerator:
  - name: inventory-service-secret
    envs:
      - .secret.env

generatorOptions:
  disableNameSuffixHash: true
